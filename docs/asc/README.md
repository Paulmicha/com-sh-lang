# ASC Living Documentation

Table of contents :

1. documentation (3 types only, as far as ASC is concerned)
    1. ideas
    1. changelogs
    1. living
1. organization
    1. globals
    1. hosts
    1. instances
    1. humans vs agents (owner ?)
    1. subjects
    1. actions
    1. hooks
    1. variants
    1. bootstrap : inc, opt-inc
    1. make shortcuts
    1. (re)init : cache, state
1. wrappers
    1. batch (synonym : parallel)
    1. chain (synonym : sequence)
    1. cronjob (TODO or just use "raw" thread wrapper instead ?)
    1. loop (TODO synonyms : deamon ? background task ? background job ? always-on ?)
    1. nested
    1. pipe
    1. remote
    1. rule (conditional and/or nested combinations)
    1. sequence
    1. stream ?
    1. thread
    1. tunnel
1. entities
    1. represents ? (why it exists)
    1. definition (scope ?)
    1. capabilities
    1. applicability ?
    1. yml includes
1. builder
    1. documenting (~ minimal OKF ? dedicated core extension ?)
    1. blueprints
    1. templates
    1. self-building (chain.able, nest.able, rule.able codegen for humans and agents)
1. usage
    1. start
    1. extend
    1. customize
    1. adapt
    1. contribute
